// @flow
import React from 'react';
import {
  View,
} from 'react-native';
import PropTypes from 'prop-types';

export const ListPaddingComponent = ({ height }) => {
  return <View style={{ height }} />;
};
